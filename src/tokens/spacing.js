const base = 8
const unit = 'px'

const spacing = {
  xxsmall: base / 2 + unit,
  xsmall: base + unit,
  small: base * 2 + unit,
  medium: base * 3 + unit,
  large: base * 4 + unit,
  xlarge: base * 5 + unit,
  xxlarge: base * 7 + unit
}

export default spacing
